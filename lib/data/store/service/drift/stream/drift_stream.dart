part of '../../service.dart';

mixin _ImplDriftStream on _ImplDrift {
  // Stream<Iterable<M>> watch<M extends DataClass>({int id = 0}) {
    // throw UnimplementedError();

    // if (id != 0) {
    //   return selectStatement<M>().watch().ma;
    // } else {
    //   return (_selectStatement<M>()..where((t) => t.id.equals(id))).watch();
    // }
  // }
}
